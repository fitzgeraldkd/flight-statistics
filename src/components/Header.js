import React from 'react';
import { AppBar, Container } from '@mui/material';

function Header() {
  return (
    <AppBar>
      <Container>
        Flight Statistics
      </Container>
    </AppBar>
  );
}

export default Header;
